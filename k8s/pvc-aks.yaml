apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: askloud-data
  namespace: askloud
spec:
  accessModes:
    - ReadWriteOnce    # Azure Disk is single-node; GUI and collector share via podAffinity
  storageClassName: azure-disk-premium
  resources:
    requests:
      storage: 10Gi
