apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: askloud-data
  namespace: askloud
spec:
  accessModes:
    - ReadWriteOnce    # GCE PD is single-node; GUI and collector share via podAffinity
  storageClassName: gce-pd-ssd
  resources:
    requests:
      storage: 10Gi
